<Window x:Class="HTTP_Request.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HTTP_Request"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBox VerticalAlignment="Center" Width="400"></TextBox>
            <Button Margin="10" Width="100">Search</Button>

            <GroupBox Margin="10,0,10,0" Header="System search">
                <StackPanel Orientation="Horizontal">
                    <RadioButton IsChecked="True">Google</RadioButton>
                    <RadioButton Margin="10,0,0,0">Bing</RadioButton>
                </StackPanel>
            </GroupBox>

            <CheckBox Margin="10" VerticalAlignment="Center">Image</CheckBox>
        </StackPanel>

        <WebBrowser Grid.Row="1"/>
    </Grid>
</Window>
